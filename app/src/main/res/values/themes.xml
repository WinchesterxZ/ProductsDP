<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DesignPatternTest" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/white</item>
        <!-- Optional: Set status bar icons to light or dark -->
        <item name="android:windowLightStatusBar" >true</item>
    </style>

    <style name="Theme.DesignPatternTest" parent="Base.Theme.DesignPatternTest" />

    <style name="Theme.RemoveSplashScreenTheme" parent="@style/Base.Theme.DesignPatternTest">
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>